import styled from "styled-components";

export const LogoWrapper = styled.div`
  z-index: 1;
  position: relative;
  background: #fff;

  h1 {
    font-size: 30px;
    color: #000;
    margin: 0 auto;
    padding: 50px 0;
  }
`;
